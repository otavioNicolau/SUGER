<?php

class DadosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('dados')->truncate();

		$dados = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('dados')->insert($dados);
	}

}
