<?php

class DiciplinasTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('diciplinas')->truncate();

		$diciplinas = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('diciplinas')->insert($diciplinas);
	}

}
