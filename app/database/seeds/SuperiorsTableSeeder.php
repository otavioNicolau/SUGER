<?php

class SuperiorsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('superiors')->truncate();

		$superiors = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('superiors')->insert($superiors);
	}

}
